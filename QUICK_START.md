# Быстрый старт - Настройка координат кабинетов

## ✅ Что уже сделано:

1. ✅ SVG файл загружен и отображается
2. ✅ Координаты кабинетов настроены (примерные, на основе анализа SVG)
3. ✅ Режим отладки добавлен для точной настройки

## 🔧 Как настроить координаты кабинетов:

### Способ 1: Использовать режим отладки (РЕКОМЕНДУЕТСЯ)

1. **Запустите приложение** на эмуляторе или устройстве
2. **Нажмите кнопку "Режим отладки"** в верхней части экрана
3. **Кликните на кабинет** на карте (например, на кабинет 201)
4. **Посмотрите координаты** - они появятся под картой в формате:
   ```
   Координаты SVG: X=280, Y=745
   ```
5. **Определите границы кабинета**:
   - Кликните на левый верхний угол кабинета → запомните координаты
   - Кликните на правый нижний угол кабинета → запомните координаты
6. **Откройте файл** `MainActivity.kt`
7. **Найдите метод** `setupRoomAreas()`
8. **Обновите координаты** для нужного кабинета:
   ```kotlin
   // Формат: createRectArea(roomId, left, top, right, bottom)
   areas.add(SvgMapHelper.createRectArea("room_201", 213f, 585f, 380f, 816.5f))
   //                                                      ↑     ↑     ↑      ↑
   //                                                  левый верхний | правый нижний
   ```

### Способ 2: Использовать готовые координаты

Координаты уже настроены в `MainActivity.kt` на основе анализа SVG файла. Если они не совпадают, используйте Способ 1.

## 📝 Пример настройки:

Допустим, вы кликнули на кабинет 201 и получили:
- Левый верхний угол: X=213, Y=585
- Правый нижний угол: X=380, Y=816

Тогда в коде будет:
```kotlin
areas.add(SvgMapHelper.createRectArea("room_201", 213f, 585f, 380f, 816f))
```

## ⚠️ Важно:

- Координаты в SVG начинаются с (0, 0) в левом верхнем углу
- Размеры вашего SVG: 1800x1000
- После изменения координат **пересоберите и запустите** приложение
- В режиме отладки координаты показываются в формате SVG (не экранные)

## 🎯 Проверка:

1. Выключите режим отладки
2. Кликните на кабинет на карте
3. Должно открыться расписание кабинета
4. Если не работает - включите режим отладки и уточните координаты

## 📚 Добавление расписания:

Откройте файл `RoomRepository.kt` и добавьте/измените расписание для кабинетов:

```kotlin
addRoom(Room(
    id = "room_201",
    number = "201",
    name = "Название кабинета",
    floor = 2,
    schedule = listOf(
        ScheduleItem("09:00-10:30", "Предмет", "Преподаватель", "Группа"),
        ScheduleItem("10:45-12:15", "Предмет", "Преподаватель", "Группа")
    )
))
```

Готово! 🎉

